# Copy to .env and fill in values before running docker compose up

# Required
POSTGRES_PASSWORD=change_me_strong_password
GRATIS_ADMIN_KEY=change_me_strong_admin_key

# Optional — defaults shown
# GRPC_PORT=9090
